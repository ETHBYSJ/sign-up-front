export class DataSelectConfig {
  constructor(name, star, content, placeholder, dangerText, options) {
    this.name = name
    this.star = star
    this.content = content
    this.placeholder = placeholder
    this.dangerText= dangerText
    this.options = options
    // this.checked
  }
}
export class DataInputConfig {
  constructor(name, star, content, placeholder, dangerText) {
    this.name = name
    this.star = star
    this.content = content
    this.placeholder = placeholder
    this.dangerText= dangerText
    // this.checked
  }
}